apiVersion: v1
kind: Service
metadata:
  name: {{ include "kaz-generic-chart.fullname" . }}
  labels:
    {{- include "kaz-generic-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kaz-generic-chart.selectorLabels" . | nindent 4 }}
